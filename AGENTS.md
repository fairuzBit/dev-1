# AGENTS.md

> Router file for AI coding agents. Keep this file short — detailed context lives in `.agents/`.

## Project

- **Name:** KonekDin
- **Detected stack:** Laravel 13 (Backend API), React SPA (Frontend), Tailwind CSS, MySQL, JWT-Auth, Spatie Permission, Gemini AI.

## Start here

- Full map of agent context: `.agents/README.md`
- Behavioral rules (coding style, communication, boundaries): `.agents/rules/`
- Project background & architecture: `.agents/context/`
- Active specs & implementation plans: `.agents/specs/`, `.agents/plans/`
- Past decisions (ADRs): `.agents/memory/decisions.md`
- Persistent learned preferences: `.agents/memory/user.md`

## Context routing

- Starting a new feature → check `.agents/specs/` for an existing spec before writing code.
- Making an architectural or library choice → read `.agents/memory/decisions.md` first, then append your decision there using the same format.
- Unsure about coding conventions → read `.agents/rules/coding.md` before writing or editing code.
- Need to understand the codebase → read `.agents/context/architecture.md` first.
- Need product/business context (why a feature exists) → check `.agents/context/product.md`.

## Boundaries

See `.agents/rules/boundaries.md` for what you may do freely, what needs confirmation first, and what is off-limits.

## Logging

If `.agents/logs/` is in use for this project, append a brief summary of what you did at the end of significant sessions. See `.agents/logs/README.md`.

---
Generated by the `init-agents` skill on 2026-06-18. This file is yours — edit freely as the project evolves.
